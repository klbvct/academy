# 🎯 Платформа профорієнтаційного тестування - Інструкція запуску

## ✅ Встановлення залежностей

```bash
npm install
```

## 🔧 Налаштування середовища

Створіть файл `.env` у корені проекту:

```env
DATABASE_URL="file:./prisma/dev.db"
JWT_SECRET="your-secret-key"
GEMINI_API_KEY="your-gemini-api-key"
LIQPAY_PUBLIC_KEY="your-liqpay-public-key"
LIQPAY_PRIVATE_KEY="your-liqpay-private-key"
EMAIL_HOST="smtp.example.com"
EMAIL_PORT=587
EMAIL_USER="your@email.com"
EMAIL_PASS="your-email-password"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

## 🗄️ База даних

Локальна розробка — створити БД та застосувати міграції:

```bash
npx prisma migrate dev --name init
```

Заповнити тестовими даними:

```bash
npm run prisma:seed
```

Продакшен — застосувати міграції без змін схеми:

```bash
npx prisma migrate deploy
```

> **Порада:** каталог `prisma/migrations` має бути в репозиторії — він містить SQL-скрипти для відтворення схеми на будь-якому сервері. Уникайте `npx prisma db push` у production.

## 🚀 Запуск сервера розробки

```bash
npm run dev
```

Сервер буде запущено на `http://localhost:3000`

## 📋 Структура проекту

```
app/
├── layout.tsx              # Кореневий layout (metadata, провайдери)
├── page.tsx                # Головна сторінка (лендінг)
├── login/                  # Сторінка входу
├── register/               # Сторінка реєстрації
├── reset-password/         # Скидання пароля
├── dashboard/              # Особистий кабінет користувача
├── tests/[id]/             # Проходження тесту та перегляд результатів
├── admin/                  # Панель адміністратора
│   └── users/[id]/         # Деталі користувача, тест-репорт
├── api/                    # Next.js API Routes
│   ├── auth/               # Реєстрація, вхід, скидання пароля
│   ├── user/               # Профіль, доступи, тести користувача
│   ├── tests/              # Відповіді, результати, рекомендації
│   ├── admin/              # Адмін-ендпоінти (користувачі, платежі, репорти)
│   └── liqpay/             # Платіжна інтеграція LiqPay
├── components/             # Shared-компоненти (AppProviders, GlobalLoadingIndicator)
├── contexts/               # React-контексти (LoadingContext)
└── hooks/                  # Кастомні хуки (useLoadingAction)

components/                 # Layout-компоненти (Header, Footer, AdminSidebar)
lib/                        # Утиліти (prisma, jwt, email, gemini, liqpay, scorers, interpretations)
prisma/
├── schema.prisma           # Схема БД
├── seed.ts                 # Seed-скрипт
└── migrations/             # Міграції (зберігати в репозиторії!)
scripts/                    # Утиліти для розробки та дебагу
public/                     # Статичні файли (зображення, іконки)
```

## 🔐 Реалізований функціонал

✅ Лендінг-сторінка з описом сервісу  
✅ Реєстрація / вхід / скидання пароля (JWT + bcryptjs)  
✅ Особистий кабінет користувача  
✅ Багатомодульне профорієнтаційне тестування  
✅ Підрахунок балів і AI-інтерпретація результатів (Google Gemini)  
✅ Експорт результатів у PDF (html2canvas + jsPDF)  
✅ Платіжна система LiqPay (доступ до тесту і результатів)  
✅ Відправка email (nodemailer)  
✅ Панель адміністратора (користувачі, платежі, тест-репорти)  
✅ Глобальний індикатор завантаження  

## 🛠️ Технологічний стек

| Шар | Технології |
|-----|-----------|
| Frontend | Next.js 14, React 18, TypeScript, Tailwind CSS |
| Backend | Next.js API Routes |
| ORM / БД | Prisma 5, SQLite (dev) / PostgreSQL (prod) |
| Auth | JWT (`jsonwebtoken`), bcryptjs |
| AI | Google Gemini (`@google/generative-ai`) |
| Платежі | LiqPay |
| Email | Nodemailer |
| PDF | html2canvas + jsPDF |
| Утиліти | axios, crypto-js |

## 🧰 Корисні скрипти

Усі скрипти знаходяться в папці `scripts/`. Приклади:

```bash
# Перевірити статус тест-доступу користувача
node scripts/check-test-access.js

# Перевірити дані користувача test@gmail.com
node scripts/check-test-user.js

# Перевірити поточні бали тесту
node scripts/check-current-scores.js

# Переглянути статус рекомендацій
node scripts/check-recommendations-status.js

# Перерахувати бали всіх модулів
node scripts/recalculate-all.js

# Повторно згенерувати рекомендації для користувача
node scripts/regenerate-user-recommendations.js

# Видати доступ до всіх тестів
node scripts/grant-all-access.js

# Перелік доступних моделей Gemini
node scripts/list-gemini-models.js
```

## ⚠️ Продакшен

- Підготуйте `DATABASE_URL` для PostgreSQL у `.env` або в налаштуваннях хостингу.
- Переконайтеся, що директорія `prisma/migrations` є в репозиторії.
- Застосуйте міграції командою `npx prisma migrate deploy`.
- Перевірте, що всі змінні середовища (`JWT_SECRET`, `GEMINI_API_KEY`, тощо) налаштовані на сервері.

---

**Статус проекту:** 🚀 MVP готовий, функціонал активно розвивається
