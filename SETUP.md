# 🎯 Платформа профорієнтаційного тестування - Інструкція запуску

## ✅ Встановлення залежностей

```bash
npm install
# або
yarn install
```

## 🚀 Запуск розробника сервера

```bash
npm run dev
# або
yarn dev
```

Сервер буде запущено на `http://localhost:3000`

## 📋 Структура проекту

```
app/
├── page.tsx              # Головна сторінка (лендінг)
├── login/
│   └── page.tsx         # Сторінка входу
├── register/
│   └── page.tsx         # Сторінка реєстрації
├── dashboard/
│   └── page.tsx         # Особистий кабінет користувача
├── api/
│   └── auth/
│       ├── register/    # API реєстрації
│       └── login/       # API входу
├── globals.css           # Глобальні стилі
└── layout.tsx            # Основний layout
```

## 🔐 Функції реалізовані

✅ **Лендінг-сторінка** з описом сервісу та функцій
✅ **Реєстрація користувача** 
✅ **Вхід в систему**
✅ **Особистий кабінет** для зареєстрованих користувачів
✅ **Простий API** для аутентифікації

## ⚠️ ВАЖЛИВО для продакшену

Поточна реалізація використовує простесь зберігання даних **в пам'яті** (`users` array). Це означає:
- Дані втрачаються при перезапуску сервера
- Паролі зберігаються у відкритому вигляді
- **НЕБЕЗПЕЧНО для продакшену**!

### Для реального проекту додайте:

1. **База даних** (PostgreSQL, MongoDB):
```bash
npm install @prisma/client
npm install -D prisma
```

2. **Хешування паролів**:
```bash
npm install bcryptjs
```

3. **Реальні JWT токени**:
```bash
npm install jsonwebtoken
npm install -D @types/jsonwebtoken
```

4. **Валідація на сервері**:
```bash
npm install zod
```

## 📝 Наступні кроки

1. 🗄️ Додати PostgreSQL БД
2. 🔒 Реалізувати хешування паролів
3. 📊 Створити таблиці для тестів і результатів
4. 🎨 Розробити тестові сторінки
5. 📈 Додати аналітику результатів

## 🛠️ Технологічний стек

- **Frontend**: Next.js 14, React 18, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes
- **Стилізація**: Tailwind CSS
- **Пакетні менеджер**: npm

## 📞 Контакти

Для питань та пропозицій зверніться до команди розробників.

---

**Статус проекту:** 📝 MVP розробляється 🚀
